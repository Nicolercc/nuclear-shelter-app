services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"
      STATIC_DIR: /app/public
      # OPENWEATHER_API_KEY: ""
      # GOOGLE_MAPS_KEY: ""
      # CORS_ORIGIN: "http://localhost:3000"
    restart: unless-stopped
