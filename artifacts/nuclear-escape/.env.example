# Copy to `.env` for local dev. Values are embedded at build time (Vite).

# Optional: full API origin when the SPA and API are on different hosts, e.g. `https://api.example.com`
# Leave unset to use same-origin `/api/*` (Vite dev proxy or reverse proxy).
# VITE_API_BASE_URL=
